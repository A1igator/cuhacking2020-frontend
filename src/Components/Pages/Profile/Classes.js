import React from 'react';
import { StyleSheet, Text, TextInput, Button, View } from 'react-native';

export default function Classes(props) {
  return (
    <View>
        {/* render all persons classes */}
    </View>
  );
}
